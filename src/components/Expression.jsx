import React from 'react'

const Expression = ({number1, number2, result}) => {

    return (
        <h1>{"" + number1 + " + " + number2 + " = " + result}</h1>
    );
}

export  default  Expression;
